//enums are sets of constants 
enum Color {
  yellow,white,green, blue
}


void main(){
  // print an element in an enum
  print(Color.blue);

  //access an element using an index
  print(Color.values[2]);
}
